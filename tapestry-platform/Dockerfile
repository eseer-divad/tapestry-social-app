# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build tapestry-platform`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tapestry-platform`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tapestry-platform && \
          adduser --system -G tapestry-platform tapestry-platform

COPY dist/tapestry-platform tapestry-platform/
RUN chown -R tapestry-platform:tapestry-platform .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tapestry-platform --omit=dev -f install

CMD [ "node", "tapestry-platform" ]
